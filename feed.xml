<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://adam-abed-abud.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://adam-abed-abud.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-19T15:11:09+00:00</updated><id>https://adam-abed-abud.github.io/feed.xml</id><title type="html">blank</title><subtitle></subtitle><entry><title type="html">Writing fast to disk in Rust</title><link href="https://adam-abed-abud.github.io/blog/2026/haipe-storage/" rel="alternate" type="text/html" title="Writing fast to disk in Rust"/><published>2026-04-19T14:11:00+00:00</published><updated>2026-04-19T14:11:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2026/haipe-storage</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2026/haipe-storage/"><![CDATA[<h1 id="how-fast-can-you-write-to-disk">How Fast Can You Write to Disk?</h1> <p>The goal of this task started as a challenge to myself to see how fast can I create a program in Rust that writes data to disk. How hard can it possibly be? Maybe I will start a series called HAIPE (= <code class="language-plaintext highlighter-rouge">How hArd can It Possible bE</code>). The goal is to obviously achieve the maximum write speed of the disk. A secondary goal (more personal) is to practice Rust development.</p> <h2 id="setup">Setup</h2> <p>My disk is a Crucial device. You can find your disk model and name using:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk <span class="nt">-o</span> name,model,serial
</code></pre></div></div> <p>Example output:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nvme0n1     CT1000P3SSD8       2321E6D9292B
|-nvme0n1p1
<span class="sb">`</span><span class="nt">-nvme0n1p2</span>
</code></pre></div></div> <p>I tested it on this CPU <code class="language-plaintext highlighter-rouge">AMD Ryzen 5 1600 Six-Core Processor</code></p> <h2 id="naive-implementation">Naive implementation</h2> <p>Let’s start with the fun part. A simple implementation may look fine, but it doesn’t ensure data is flushed to disk.</p> <p>Result</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Read 1073741824 bytes
Wrote 1073741824 bytes <span class="k">in </span>1.110s
</code></pre></div></div> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="n">fs</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">fs</span><span class="p">::</span><span class="nf">read</span><span class="p">(</span><span class="s">"input"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read input file"</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Read {} bytes"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">());</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>
    <span class="nn">fs</span><span class="p">::</span><span class="nf">write</span><span class="p">(</span><span class="s">"output/copy"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">data</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write file"</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span><span class="s">"Wrote {} bytes in {:.3?}"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">(),</span> <span class="n">elapsed</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="adding-the-flush-to-disk">Adding the flush to disk</h2> <p>The following code adds the direct flushing to disk.</p> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">fs</span><span class="p">::</span><span class="nf">read</span><span class="p">(</span><span class="s">"input"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read input file"</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Read {} bytes"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">());</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">let</span> <span class="k">mut</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="s">"output/copy"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create file"</span><span class="p">);</span>
    <span class="c1">// Write to file and sync so that OS write immediately</span>
    <span class="c1">// write_all makes sure that the all the data have been written</span>
    <span class="c1">// sync_all is the equivalent as calling the POSIX fsync(). It is a blocking call</span>
    <span class="n">file</span><span class="nf">.write_all</span><span class="p">(</span><span class="o">&amp;</span><span class="n">data</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write data"</span><span class="p">);</span>
    <span class="n">file</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span><span class="s">"Wrote {} bytes in {:.3?}"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">(),</span> <span class="n">elapsed</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Here is the output that I got.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ cargo run
   Compiling storage_writer v0.1.0 <span class="o">(</span>/home/adam/Desktop/projects/HAIPE/storage_writer<span class="o">)</span>
    Finished dev <span class="o">[</span>unoptimized + debuginfo] target<span class="o">(</span>s<span class="o">)</span> <span class="k">in </span>0.47s
     Running <span class="sb">`</span>target/debug/storage_writer<span class="sb">`</span>
Read 1073741824 bytes
Wrote 1073741824 bytes <span class="k">in </span>1.443s
</code></pre></div></div> <p>It is obviously slower because the OS needs to flush immediately before returning.</p> <p>Let’s add some page caching and measure the throughput values. After the first run, the OS keeps the 1 GB input file in the page cache (RAM). So fs::read isn’t really reading from disk: it’s copying from cached memory. And the output might also benefit from the page cache if the kernel hasn’t flushed the previous run’s data yet.</p> <p>Run the following command to drop the page cache:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sh <span class="nt">-c</span> <span class="s1">'echo 3 &gt; /proc/sys/vm/drop_caches'</span>
</code></pre></div></div> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="n">fs</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">fs</span><span class="p">::</span><span class="nf">read</span><span class="p">(</span><span class="s">"input"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read input file"</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Read {} bytes"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">());</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">let</span> <span class="k">mut</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="s">"output/copy"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create file"</span><span class="p">);</span>
    <span class="c1">// Write to file and sync so that OS write immediately</span>
    <span class="c1">// write_all makes sure that the all the data have been written</span>
    <span class="c1">// sync_all is the equivalent as calling the POSIX fsync(). It is a blocking call</span>
    <span class="n">file</span><span class="nf">.write_all</span><span class="p">(</span><span class="o">&amp;</span><span class="n">data</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write data"</span><span class="p">);</span>
    <span class="n">file</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>
    <span class="k">let</span> <span class="n">bytes</span> <span class="o">=</span> <span class="n">data</span><span class="nf">.len</span><span class="p">()</span> <span class="k">as</span> <span class="nb">f64</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">mb</span> <span class="o">=</span> <span class="n">bytes</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1024.0</span> <span class="o">*</span> <span class="mf">1024.0</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">secs</span> <span class="o">=</span> <span class="n">elapsed</span><span class="nf">.as_secs_f64</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span><span class="s">"Wrote {:.1} MB in {:.3?} ({:.1} MB/s)"</span><span class="p">,</span> <span class="n">mb</span><span class="p">,</span> <span class="n">elapsed</span><span class="p">,</span> <span class="n">mb</span> <span class="o">/</span> <span class="n">secs</span><span class="p">);</span>

<span class="p">}</span>
</code></pre></div></div> <h2 id="comparison-with-dd-buffered-writes">Comparison with DD? Buffered writes?</h2> <p>Creating a file using /dev/random is CPU bound:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">dd </span><span class="k">if</span><span class="o">=</span>/dev/random <span class="nv">of</span><span class="o">=</span>input <span class="nv">bs</span><span class="o">=</span>1G <span class="nv">count</span><span class="o">=</span>1
</code></pre></div></div> <p>Instead you should be doing something like:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">dd </span><span class="k">if</span><span class="o">=</span>/dev/zero <span class="nv">of</span><span class="o">=</span>output/dd_test <span class="nv">bs</span><span class="o">=</span>1M <span class="nv">count</span><span class="o">=</span>1024 <span class="nv">conv</span><span class="o">=</span>fdatasync
</code></pre></div></div> <p>That writes 1 GB of zeros (no CPU bottleneck) and <code class="language-plaintext highlighter-rouge">conv=fdatasync</code> forces a flush like our <code class="language-plaintext highlighter-rouge">sync_all()</code>. In my case I got the following:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ <span class="nb">dd </span><span class="k">if</span><span class="o">=</span>/dev/zero <span class="nv">of</span><span class="o">=</span>output/dd_test <span class="nv">bs</span><span class="o">=</span>1M <span class="nv">count</span><span class="o">=</span>1024 <span class="nv">conv</span><span class="o">=</span>fdatasync
1024+0 records <span class="k">in
</span>1024+0 records out
1073741824 bytes <span class="o">(</span>1.1 GB, 1.0 GiB<span class="o">)</span> copied, 1.24273 s, 864 MB/s
</code></pre></div></div> <p>Right now we do one giant 1 GB write_all() call. In real systems, you often write in chunks (e.g., streaming data as it arrives). Let’s see how chunk size affects performance.</p> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">BufWriter</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::{</span><span class="n">env</span><span class="p">,</span> <span class="n">fs</span><span class="p">};</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">args</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nn">env</span><span class="p">::</span><span class="nf">args</span><span class="p">()</span><span class="nf">.collect</span><span class="p">();</span>

    <span class="k">if</span> <span class="n">args</span><span class="nf">.len</span><span class="p">()</span> <span class="o">!=</span> <span class="mi">3</span> <span class="p">{</span>
        <span class="nd">eprintln!</span><span class="p">(</span><span class="s">"Usage: {} &lt;input&gt; &lt;output&gt;"</span><span class="p">,</span> <span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>
        <span class="nn">std</span><span class="p">::</span><span class="nn">process</span><span class="p">::</span><span class="nf">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">let</span> <span class="n">input_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">];</span>
    <span class="k">let</span> <span class="n">output_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>

    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">fs</span><span class="p">::</span><span class="nf">read</span><span class="p">(</span><span class="s">"input"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read input file"</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Read {} bytes"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">());</span>

    <span class="k">let</span> <span class="n">chunk_size</span> <span class="o">=</span> <span class="mi">4096</span><span class="p">;</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">let</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="n">output_path</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create file"</span><span class="p">);</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">writer</span> <span class="o">=</span> <span class="nn">BufWriter</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">file</span><span class="p">);</span>

    <span class="k">for</span> <span class="n">chunk</span> <span class="k">in</span> <span class="n">data</span><span class="nf">.chunks</span><span class="p">(</span><span class="n">chunk_size</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">writer</span><span class="nf">.write_all</span><span class="p">(</span><span class="n">chunk</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write chunk"</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="n">writer</span><span class="nf">.flush</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to flush"</span><span class="p">);</span>
    <span class="n">writer</span><span class="nf">.get_ref</span><span class="p">()</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>
    <span class="k">let</span> <span class="n">bytes</span> <span class="o">=</span> <span class="n">data</span><span class="nf">.len</span><span class="p">()</span> <span class="k">as</span> <span class="nb">f64</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">mb</span> <span class="o">=</span> <span class="n">bytes</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1024.0</span> <span class="o">*</span> <span class="mf">1024.0</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">secs</span> <span class="o">=</span> <span class="n">elapsed</span><span class="nf">.as_secs_f64</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span>
        <span class="s">"Wrote {:.1} MB in {:.3?} ({:.1} MB/s) [chunk={}]"</span><span class="p">,</span>
        <span class="n">mb</span><span class="p">,</span>
        <span class="n">elapsed</span><span class="p">,</span>
        <span class="n">mb</span> <span class="o">/</span> <span class="n">secs</span><span class="p">,</span>
        <span class="n">chunk_size</span>
    <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Notice in the above code how expressive Rust is (I’m still a beginner on Rust): <code class="language-plaintext highlighter-rouge">data.chunks(chunk_size)</code> returns an iterator of slices or how the buffered writer takes ownership of the file (it moves in).</p> <p>Here is the output that I got:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ target/debug/storage_writer input output/copy
Read 1073741824 bytes
Wrote 1024.0 MB <span class="k">in </span>1.560s <span class="o">(</span>656.3 MB/s<span class="o">)</span> <span class="o">[</span><span class="nv">chunk</span><span class="o">=</span>4096]
</code></pre></div></div> <h2 id="lets-write-chunks-directly">Let’s write chunks directly!</h2> <p>Let’s change the code such that we write chunks directly to disk.</p> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::{</span><span class="n">env</span><span class="p">,</span> <span class="n">fs</span><span class="p">};</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">args</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nn">env</span><span class="p">::</span><span class="nf">args</span><span class="p">()</span><span class="nf">.collect</span><span class="p">();</span>

    <span class="k">if</span> <span class="n">args</span><span class="nf">.len</span><span class="p">()</span> <span class="o">!=</span> <span class="mi">3</span> <span class="p">{</span>
        <span class="nd">eprintln!</span><span class="p">(</span><span class="s">"Usage: {} &lt;input&gt; &lt;output&gt;"</span><span class="p">,</span> <span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>
        <span class="nn">std</span><span class="p">::</span><span class="nn">process</span><span class="p">::</span><span class="nf">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">let</span> <span class="n">input_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">];</span>
    <span class="k">let</span> <span class="n">output_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>

    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">fs</span><span class="p">::</span><span class="nf">read</span><span class="p">(</span><span class="s">"input"</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read input file"</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Read {} bytes"</span><span class="p">,</span> <span class="n">data</span><span class="nf">.len</span><span class="p">());</span>

    <span class="k">let</span> <span class="n">chunk_size</span> <span class="o">=</span> <span class="mi">8</span><span class="o">*</span><span class="mi">1024</span><span class="o">*</span><span class="mi">1024</span><span class="p">;</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">let</span> <span class="k">mut</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="n">output_path</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create file"</span><span class="p">);</span>

    <span class="k">for</span> <span class="n">chunk</span> <span class="k">in</span> <span class="n">data</span><span class="nf">.chunks</span><span class="p">(</span><span class="n">chunk_size</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">file</span><span class="nf">.write_all</span><span class="p">(</span><span class="n">chunk</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write chunk"</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="n">file</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>
    <span class="k">let</span> <span class="n">bytes</span> <span class="o">=</span> <span class="n">data</span><span class="nf">.len</span><span class="p">()</span> <span class="k">as</span> <span class="nb">f64</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">mb</span> <span class="o">=</span> <span class="n">bytes</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1024.0</span> <span class="o">*</span> <span class="mf">1024.0</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">secs</span> <span class="o">=</span> <span class="n">elapsed</span><span class="nf">.as_secs_f64</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span>
        <span class="s">"Wrote {:.1} MB in {:.3?} ({:.1} MB/s) [chunk={}]"</span><span class="p">,</span>
        <span class="n">mb</span><span class="p">,</span>
        <span class="n">elapsed</span><span class="p">,</span>
        <span class="n">mb</span> <span class="o">/</span> <span class="n">secs</span><span class="p">,</span>
        <span class="n">chunk_size</span>
    <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Here is the output that I got:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ target/debug/storage_writer input output/copy
Read 1073741824 bytes
Wrote 1024.0 MB in 1.480s (691.7 MB/s) [chunk=8388608]
</code></pre></div></div> <h2 id="another-approach">Another approach</h2> <p>Let’s try another approach. We could stream the data instead of copying everything into memory.</p> <p>Note that this crashes for 10GB file writing because the OS killed your process. That’s the Linux OOM (Out of Memory) killer. <code class="language-plaintext highlighter-rouge">fs::read()</code> tries to load the entire 10 GB file into RAM, and my system doesn’t have enough.</p> <p>Instead of <code class="language-plaintext highlighter-rouge">fs::read()</code> (load everything), we open both files and copy chunk by chunk. This uses only chunk_size bytes of RAM, no matter how big the file.</p> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="n">env</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Read</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>

<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">args</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nn">env</span><span class="p">::</span><span class="nf">args</span><span class="p">()</span><span class="nf">.collect</span><span class="p">();</span>

    <span class="k">if</span> <span class="n">args</span><span class="nf">.len</span><span class="p">()</span> <span class="o">!=</span> <span class="mi">3</span> <span class="p">{</span>
        <span class="nd">eprintln!</span><span class="p">(</span><span class="s">"Usage: {} &lt;input&gt; &lt;output&gt;"</span><span class="p">,</span> <span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>
        <span class="nn">std</span><span class="p">::</span><span class="nn">process</span><span class="p">::</span><span class="nf">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="k">let</span> <span class="n">input_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">];</span>
    <span class="k">let</span> <span class="n">output_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>

    <span class="k">let</span> <span class="n">chunk_size</span> <span class="o">=</span> <span class="mi">8</span> <span class="o">*</span> <span class="mi">1024</span> <span class="o">*</span> <span class="mi">1024</span><span class="p">;</span>

    <span class="k">let</span> <span class="k">mut</span> <span class="n">reader</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">open</span><span class="p">(</span><span class="n">input_path</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to open input"</span><span class="p">);</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">writer</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="n">output_path</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create output"</span><span class="p">);</span>

    <span class="k">let</span> <span class="k">mut</span> <span class="n">buffer</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="mi">0u8</span><span class="p">;</span> <span class="n">chunk_size</span><span class="p">];</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">total_bytes</span><span class="p">:</span> <span class="nb">u64</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

    <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">loop</span> <span class="p">{</span>
        <span class="k">let</span> <span class="n">bytes_read</span> <span class="o">=</span> <span class="n">reader</span><span class="nf">.read</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read"</span><span class="p">);</span>
        <span class="k">if</span> <span class="n">bytes_read</span> <span class="o">==</span> <span class="mi">0</span> <span class="p">{</span>
            <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="n">writer</span>
            <span class="nf">.write_all</span><span class="p">(</span><span class="o">&amp;</span><span class="n">buffer</span><span class="p">[</span><span class="o">..</span><span class="n">bytes_read</span><span class="p">])</span>
            <span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write"</span><span class="p">);</span>
        <span class="n">total_bytes</span> <span class="o">+=</span> <span class="n">bytes_read</span> <span class="k">as</span> <span class="nb">u64</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="n">writer</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

    <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>
    <span class="k">let</span> <span class="n">mb</span> <span class="o">=</span> <span class="n">total_bytes</span> <span class="k">as</span> <span class="nb">f64</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1024.0</span> <span class="o">*</span> <span class="mf">1024.0</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">secs</span> <span class="o">=</span> <span class="n">elapsed</span><span class="nf">.as_secs_f64</span><span class="p">();</span>

    <span class="nd">println!</span><span class="p">(</span>
        <span class="s">"Wrote {:.1} MB in {:.3?} ({:.1} MB/s) [chunk={}]"</span><span class="p">,</span>
        <span class="n">mb</span><span class="p">,</span>
        <span class="n">elapsed</span><span class="p">,</span>
        <span class="n">mb</span> <span class="o">/</span> <span class="n">secs</span><span class="p">,</span>
        <span class="n">chunk_size</span>
    <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Is there a difference in <code class="language-plaintext highlighter-rouge">Release build</code>? Let’s see.</p> <p>We have been running debug builds this whole time. Debug mode disables optimizations and adds bounds checks on every array access. For our tight loop doing millions of slice operations, this matters. In C++ terms, this is the equivalent between <code class="language-plaintext highlighter-rouge">-O0</code> and <code class="language-plaintext highlighter-rouge">-O2</code>.</p> <p>Here is the output I got (very surprising to me!)</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ target/release/storage_writer input output/copy
Wrote 10240.0 MB <span class="k">in </span>76.337s <span class="o">(</span>134.1 MB/s<span class="o">)</span> <span class="o">[</span><span class="nv">chunk</span><span class="o">=</span>8388608]
</code></pre></div></div> <h2 id="yet-another-approach-write-a-const-block">Yet another approach. Write a const block</h2> <p>Are input and output/copy on the same physical disk? If so, the disk head (or NVMe controller) is ping-ponging between reading and writing, which could kill throughput.</p> <p>Here we write a constant quantity directly to file.</p> <p>Code:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="n">env</span><span class="p">;</span>
  <span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">fs</span><span class="p">::</span><span class="n">File</span><span class="p">;</span>
  <span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="n">Write</span><span class="p">;</span>
  <span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="n">Instant</span><span class="p">;</span>

  <span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
      <span class="k">let</span> <span class="n">args</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nn">env</span><span class="p">::</span><span class="nf">args</span><span class="p">()</span><span class="nf">.collect</span><span class="p">();</span>

      <span class="k">if</span> <span class="n">args</span><span class="nf">.len</span><span class="p">()</span> <span class="o">!=</span> <span class="mi">3</span> <span class="p">{</span>
          <span class="nd">eprintln!</span><span class="p">(</span><span class="s">"Usage: {} &lt;output&gt; &lt;size_mb&gt;"</span><span class="p">,</span> <span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>
          <span class="nn">std</span><span class="p">::</span><span class="nn">process</span><span class="p">::</span><span class="nf">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
      <span class="p">}</span>

      <span class="k">let</span> <span class="n">output_path</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">];</span>
      <span class="k">let</span> <span class="n">size_mb</span><span class="p">:</span> <span class="nb">u64</span> <span class="o">=</span> <span class="n">args</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span><span class="nf">.parse</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Invalid size in MB"</span><span class="p">);</span>
      <span class="k">let</span> <span class="n">total_bytes</span> <span class="o">=</span> <span class="n">size_mb</span> <span class="o">*</span> <span class="mi">1024</span> <span class="o">*</span> <span class="mi">1024</span><span class="p">;</span>

      <span class="k">let</span> <span class="n">chunk_size</span> <span class="o">=</span> <span class="mi">8</span> <span class="o">*</span> <span class="mi">1024</span> <span class="o">*</span> <span class="mi">1024</span><span class="p">;</span>
      <span class="k">let</span> <span class="n">buffer</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="mi">0xABu8</span><span class="p">;</span> <span class="n">chunk_size</span><span class="p">];</span>

      <span class="k">let</span> <span class="k">mut</span> <span class="n">file</span> <span class="o">=</span> <span class="nn">File</span><span class="p">::</span><span class="nf">create</span><span class="p">(</span><span class="n">output_path</span><span class="p">)</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to create output"</span><span class="p">);</span>
      <span class="k">let</span> <span class="k">mut</span> <span class="n">written</span><span class="p">:</span> <span class="nb">u64</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

      <span class="k">let</span> <span class="n">start</span> <span class="o">=</span> <span class="nn">Instant</span><span class="p">::</span><span class="nf">now</span><span class="p">();</span>

      <span class="k">while</span> <span class="n">written</span> <span class="o">&lt;</span> <span class="n">total_bytes</span> <span class="p">{</span>
          <span class="k">let</span> <span class="n">remaining</span> <span class="o">=</span> <span class="p">(</span><span class="n">total_bytes</span> <span class="o">-</span> <span class="n">written</span><span class="p">)</span> <span class="k">as</span> <span class="nb">usize</span><span class="p">;</span>
          <span class="k">let</span> <span class="n">to_write</span> <span class="o">=</span> <span class="n">remaining</span><span class="nf">.min</span><span class="p">(</span><span class="n">chunk_size</span><span class="p">);</span>
          <span class="n">file</span><span class="nf">.write_all</span><span class="p">(</span><span class="o">&amp;</span><span class="n">buffer</span><span class="p">[</span><span class="o">..</span><span class="n">to_write</span><span class="p">])</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to write"</span><span class="p">);</span>
          <span class="n">written</span> <span class="o">+=</span> <span class="n">to_write</span> <span class="k">as</span> <span class="nb">u64</span><span class="p">;</span>
      <span class="p">}</span>

      <span class="n">file</span><span class="nf">.sync_all</span><span class="p">()</span><span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to sync"</span><span class="p">);</span>

      <span class="k">let</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">start</span><span class="nf">.elapsed</span><span class="p">();</span>
      <span class="k">let</span> <span class="n">mb</span> <span class="o">=</span> <span class="n">written</span> <span class="k">as</span> <span class="nb">f64</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1024.0</span> <span class="o">*</span> <span class="mf">1024.0</span><span class="p">);</span>
      <span class="k">let</span> <span class="n">secs</span> <span class="o">=</span> <span class="n">elapsed</span><span class="nf">.as_secs_f64</span><span class="p">();</span>

      <span class="nd">println!</span><span class="p">(</span>
          <span class="s">"Wrote {:.1} MB in {:.3?} ({:.1} MB/s) [chunk={}]"</span><span class="p">,</span>
          <span class="n">mb</span><span class="p">,</span> <span class="n">elapsed</span><span class="p">,</span> <span class="n">mb</span> <span class="o">/</span> <span class="n">secs</span><span class="p">,</span> <span class="n">chunk_size</span>
      <span class="p">);</span>
  <span class="p">}</span>
</code></pre></div></div> <p>Run it like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cargo run <span class="nt">--release</span> <span class="nt">--</span> output/copy 10240
</code></pre></div></div> <p>And here is what I got:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ cargo run <span class="nt">--release</span> <span class="nt">--</span> output/copy 10240
    Finished release <span class="o">[</span>optimized] target<span class="o">(</span>s<span class="o">)</span> <span class="k">in </span>0.05s
     Running <span class="sb">`</span>target/release/storage_writer output/copy 10240<span class="sb">`</span>
Wrote 10240.0 MB <span class="k">in </span>89.893s <span class="o">(</span>113.9 MB/s<span class="o">)</span> <span class="o">[</span><span class="nv">chunk</span><span class="o">=</span>8388608]
</code></pre></div></div> <h2 id="wrap-up">Wrap-up</h2> <p>Interesting insights:</p> <ol> <li>My disk is 96% full — only 41 GB free. NVMe SSDs slow down dramatically when nearly full because the controller runs out of free NAND blocks and has to do garbage collection during writes.</li> <li>268 MB/s for 1 GB (down from 700 MB/s earlier): earlier runs were hot in the page cache. This is closer to your real disk speed under pressure. So ~114-268 MB/s is the actual sustained write speed given the disk state. That’s expected for a nearly-full NVMe.</li> </ol>]]></content><author><name></name></author><summary type="html"><![CDATA[How hard can it possibly be? (HAIPE)]]></summary></entry><entry><title type="html">Git Kung Fu</title><link href="https://adam-abed-abud.github.io/blog/2026/git-kung-fu/" rel="alternate" type="text/html" title="Git Kung Fu"/><published>2026-04-10T21:31:00+00:00</published><updated>2026-04-10T21:31:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2026/git-kung-fu</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2026/git-kung-fu/"><![CDATA[<h2 id="git-kung-fu">Git Kung Fu</h2> <p>It turns out the commit history tells a story. Who touched what, where things break, and which parts everyone avoids. Inspired by blog title “The Git Commands I Run Before Reading Any Code” (here is the link <a href="https://piechowski.io/post/git-commands-before-reading-code/">link</a>), a few simple commands can reveal “where a codebase hurts before you open a single file.”</p> <p>This is super cool. I like to call it Git Kung Fu. They are nice command line tools to go deeper in the code. Somehow this information is coumbersome to get on the Github/Gitlab web interface.</p> <h3 id="some-tricks">Some tricks</h3> <p>The following shows the files that change the most.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--format</span><span class="o">=</span>format: <span class="nt">--name-only</span> <span class="nt">--since</span><span class="o">=</span><span class="s2">"1 year ago"</span> | <span class="nb">sort</span> | <span class="nb">uniq</span> <span class="nt">-c</span> | <span class="nb">sort</span> <span class="nt">-nr</span> | <span class="nb">head</span> <span class="nt">-20</span>
</code></pre></div></div> <p>Who has created the most code? Here you go: <code class="language-plaintext highlighter-rouge">git shortlog -sn --no-merges</code> . This is however skewed measure because it is affected if you import a library or copy stuff here and there across the repo.</p> <p>My absolute favorite is the following: see which branches someone has been active on recently</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-r</span> | <span class="nb">grep</span> <span class="nt">-v</span> <span class="s1">'\-&gt;'</span> | xargs <span class="nt">-I</span> <span class="o">{}</span> git log <span class="nt">-1</span> <span class="nt">--format</span><span class="o">=</span><span class="s2">"%ci %an {}"</span> <span class="o">{}</span> | <span class="nb">grep</span> <span class="s2">"adam-abed-abud"</span> | <span class="nb">awk</span> <span class="s1">'$4 &gt;= "'</span><span class="si">$(</span><span class="nb">date</span> <span class="nt">-d</span> <span class="s1">'2 weeks ago'</span> <span class="s1">'+%Y-%m-%d'</span><span class="si">)</span><span class="s1">'"'</span> | <span class="nb">sort</span> <span class="nt">-r</span>
</code></pre></div></div> <p>This one feels like time-travel debugging. You instantly see where someone has been working across remote branches in the last couple of weeks.</p> <p>To map out contributors across all remotes:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--remotes</span> <span class="nt">--format</span><span class="o">=</span><span class="s2">"%ae %an"</span> | <span class="nb">sort</span> <span class="nt">-u</span>
</code></pre></div></div> <p>And to zoom in on a specific person:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--remotes</span> <span class="nt">--format</span><span class="o">=</span><span class="s2">"%ae %an"</span> | <span class="nb">sort</span> <span class="nt">-u</span> | <span class="nb">grep</span> <span class="nt">-i</span> USERNAME
</code></pre></div></div> <p>Simple, but powerful. It answers questions like:</p> <p>Who is actually active right now? Are there ghost contributors?</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Cool tricks with git]]></summary></entry><entry><title type="html">How I Review C++.</title><link href="https://adam-abed-abud.github.io/blog/2026/CPP/" rel="alternate" type="text/html" title="How I Review C++."/><published>2026-02-21T09:31:00+00:00</published><updated>2026-02-21T09:31:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2026/CPP</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2026/CPP/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>I did not really learn C++ by writing it. I first learned it by reviewing it, looking at other people’s code and then by writing code myself.</p> <p>At some point, reviews stopped being about style and started being about risk. In C++, risk hides in many different places. Most of the time the code can compile, pass tests, and still be wrong in a way that only shows up later on.</p> <p>Here is what I typically look for when reviewing C++ code. The reason why I write it is because I see a lot of people nit-picking on useless things and style rather than solving a particular problem.</p> <h3 id="correctness-and-invariants-first">Correctness and Invariants First</h3> <p>Before performance, before elegance, I look for invariants.</p> <p>What must be true before this function runs. What is guaranteed after. I scan for classic undefined behavior traps. Out-of-bounds access, dangling references, invalidated iterators, signed overflow, use-after-move, and lifetime issues with views like <code class="language-plaintext highlighter-rouge">std::span</code> or <code class="language-plaintext highlighter-rouge">std::string_view</code>.</p> <p>If the invariants are not explicit, I push to make them explicit. Assertions are cheaper than outages.</p> <h3 id="ownership-and-lifetimes">Ownership and Lifetimes</h3> <p>In C++, types are documentation. Ownership should be obvious. <code class="language-plaintext highlighter-rouge">std::unique_ptr</code> for sole ownership. <code class="language-plaintext highlighter-rouge">std::shared_ptr</code> only when sharing is real. Raw pointers and references for non-owning relationships. Views are powerful, but only if the underlying storage outlives them. I typically think of “who owns what”.</p> <h3 id="raii-and-exception-safety">RAII and Exception Safety</h3> <p>Manual resource management is a smell. Resources should be tied to objects. Construction acquires. Destruction releases. That is the RAII principle. I also check exception guarantees. At minimum, does the code provide the basic guarantee. No leaks. No half-updated objects. Destructors should not throw.</p> <h3 id="interfaces-that-resist-misuse">Interfaces That Resist Misuse</h3> <p>Good C++ APIs make the wrong thing hard. I prefer explicit interfaces and strong types over loosely defined integers and strings. If an API is easy to misuse, someone eventually will. Of course this depends on the context.</p> <h3 id="consistency-concurrency-and-performance">Consistency, Concurrency, and Performance</h3> <p>Error handling must be consistent. Exceptions, <code class="language-plaintext highlighter-rouge">std::expected</code> (super cool!), or error codes. Pick a strategy and apply it systematically.</p> <p>With concurrency, I slow down. I look for shared mutable state, unclear lifetimes across threads, and inconsistent locking. Subtle races are worse than loud crashes.</p> <p>Performance comes last. I focus on algorithmic complexity and obvious waste in hot paths. Not premature micro-optimizations. I care about them only later on, unless they are obvious.</p> <h3 id="what-i-learned-from-reviewing-c">What I learned from reviewing C++</h3> <p>Reviewing C++ made me more disciplined. I care less about cleverness and more about guarantees. What does this code promise. What does it assume. What happens when it fails.</p> <p>Also and most importantly when reviewing, I try to test the application/feature myself and not just reviewing the code.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Things that I look for when reviewing C++ code]]></summary></entry><entry><title type="html">Back of the envelope math for Computers</title><link href="https://adam-abed-abud.github.io/blog/2025/napk/" rel="alternate" type="text/html" title="Back of the envelope math for Computers"/><published>2025-12-30T11:55:00+00:00</published><updated>2025-12-30T11:55:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2025/napk</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2025/napk/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>I rely constantly on back of the envelope math calculations or as <a href="https://sirupsen.com/">Simon Eskildsen</a> would say “napkin math”. This is something I usually do before I open a profiler, before I write a benchmark, before I argue about micro-optimizations with some colleagues. A rough mental model of computer speeds saves me from bad designs and wasted time. This is a practice very commonly used in Physics where you just look at the order of magnitude and try to model the behaviour of a system.</p> <p>This post is my personal cheat sheet for latency and bandwidth across CPU, RAM, SSDs, and HDDs, plus a few examples that can be really useful when reviewing a design document or talking about system architecture.</p> <h2 id="the-numbers-i-actually-remember">The Numbers I Actually Remember</h2> <p>I do <strong>not</strong> remember exact specs. I remember orders of magnitude.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>| Component       | Latency (approx) | Bandwidth (approx) |
| --------------- | ---------------- | ------------------ |
| CPU register    | ~0.3 ns          | Enormous           |
| L1 cache        | ~1 ns            | 1–2 TB/s           |
| L2 cache        | ~4 ns            | Hundreds of GB/s   |
| L3 cache        | ~10–15 ns        | 100+ GB/s          |
| RAM             | ~80–120 ns       | 25–80 GB/s         |
| NVMe SSD        | ~50–150 µs       | 3–7 GB/s           |
| SATA SSD        | ~100 µs          | ~500 MB/s          |
| HDD             | ~5–10 ms         | 100–200 MB/s       |
</code></pre></div></div> <p>What matters. Each level down the hierarchy costs roughly <strong>10× more latency</strong>.</p> <p>That single rule already explains most performance surprises.</p> <hr/> <h2 id="latency-vs-bandwidth-where-i-see-people-get-tricked">Latency vs Bandwidth. Where I See People Get Tricked</h2> <p>Latency answers. <em>How long until the first byte shows up?</em><br/> Bandwidth answers. <em>How fast can I stream once it does?</em></p> <p>I have personally made the mistake of optimizing for bandwidth when latency was the real bottleneck.</p> <p>This is why a cache miss hurts even when I am touching just a few bytes.</p> <hr/> <h2 id="math-examples">Math Examples</h2> <h3 id="example-1-the-linked-list-mistake">Example 1. The linked list mistake</h3> <p>Let’s look at an example of a linked list for a hot path because “the data structure was clean”.</p> <p>10 million nodes, pointer chasing in RAM.</p> <ul> <li>One RAM access ≈ 100 ns</li> <li>10M × 100 ns = <strong>1 second</strong></li> </ul> <p>Try to replace it with a flat array.</p> <ul> <li>Sequential scan, prefetch works</li> <li>~40 MB / 40 GB/s ≈ <strong>1 millisecond</strong></li> </ul> <p>Same data. Three orders of magnitude difference. Sometimes having pointer-heavy structures in performance-critical code is not needed.</p> <h3 id="example-2-its-just-a-disk-read">Example 2. “It’s just a disk read”</h3> <p>I once had a task doing ~100 random reads per request on an SSD.</p> <ul> <li>100 × 100 µs = <strong>10 ms</strong></li> </ul> <p>This looks fine if you take it by itself. <strong>Then traffic spiked</strong>.</p> <p>Try to test the same workload but on an HDD and you see the following:</p> <ul> <li>100 × 8 ms = <strong>800 ms</strong></li> </ul> <p>That experiment permanently changed my intuition around random I/O.</p> <h3 id="example-3-cpu-cycles-vs-memory">Example 3. CPU cycles vs memory</h3> <p>On a 3 GHz CPU.</p> <ul> <li>1 cycle ≈ 0.33 ns</li> <li>RAM access ≈ 300 cycles</li> <li>Branch misprediction ≈ 15 cycles</li> </ul> <p>This teaches you that obsessing over instruction counts is useless if you forget about cache misses</p> <h2 id="tiny-c-benchmarks-i-use-for-reality-checks">Tiny C++ Benchmarks I Use for Reality Checks</h2> <p>These are <strong>not</strong> scientific benchmarks. I use them to keep my intuition honest.</p> <p>Compile with optimizations enabled. <code class="language-plaintext highlighter-rouge">-O2</code> or <code class="language-plaintext highlighter-rouge">-O3</code>. (play with them both)</p> <h3 id="prototype-code-to-perform-back-of-envelope-math-memory-latency">Prototype code to perform back of envelope math: memory latency</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;chrono&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;algorithm&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">const</span> <span class="kt">size_t</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">1'000'000</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">size_t</span><span class="o">&gt;</span> <span class="n">next</span><span class="p">(</span><span class="n">N</span><span class="p">);</span>

    <span class="n">std</span><span class="o">::</span><span class="n">mt19937_64</span> <span class="n">rng</span><span class="p">(</span><span class="mi">1234</span><span class="p">);</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">N</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">next</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">i</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shuffle</span><span class="p">(</span><span class="n">next</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">next</span><span class="p">.</span><span class="n">end</span><span class="p">(),</span> <span class="n">rng</span><span class="p">);</span>

    <span class="kt">size_t</span> <span class="n">idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">auto</span> <span class="n">start</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">high_resolution_clock</span><span class="o">::</span><span class="n">now</span><span class="p">();</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">N</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">next</span><span class="p">[</span><span class="n">idx</span><span class="p">];</span>
    <span class="p">}</span>
    <span class="k">auto</span> <span class="n">end</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">high_resolution_clock</span><span class="o">::</span><span class="n">now</span><span class="p">();</span>

    <span class="k">auto</span> <span class="n">ns</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">duration_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">nanoseconds</span><span class="o">&gt;</span><span class="p">(</span><span class="n">end</span> <span class="o">-</span> <span class="n">start</span><span class="p">).</span><span class="n">count</span><span class="p">();</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Average access latency: "</span>
              <span class="o">&lt;&lt;</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">(</span><span class="n">ns</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span> <span class="o">&lt;&lt;</span> <span class="s">" ns</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="conclusion">Conclusion</h2> <p>Here are some mental rules I actually use:</p> <ul> <li>Cache misses dominate performance before arithmetic does</li> <li>Sequential access beats random access</li> <li>One disk seek equals millions of CPU instructions</li> </ul> <p>Bandwidth affects throughput, latency affects responsiveness</p> <p>Back of the envelope math is not about being precise. It is about being directionally correct early. When my estimate says milliseconds but reality says seconds, then you have some debugging to do <code class="language-plaintext highlighter-rouge">:-)</code></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Latency, Bandwidth, and Why Orders of Magnitude Matter]]></summary></entry><entry><title type="html">Modern C++ guide</title><link href="https://adam-abed-abud.github.io/blog/2025/guide/" rel="alternate" type="text/html" title="Modern C++ guide"/><published>2025-06-06T17:55:00+00:00</published><updated>2025-06-06T17:55:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2025/guide</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2025/guide/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>I recently dedicated some time to learn some concepts of modern C++. And since I was on it why not summarizing the recent development of modern C++? The real reason is that I felt I was always using the same features of modern C++ but I felt that there were a lot of topics that I did not master or that I was not even aware of, especially in C++20.What sets C++20 apart for me is how it reshapes template programming and functional-style operations in ways that feel both futuristic and incredibly practical.</p> <p>Here is my free pdf guidebook on modern C++! <a href="/assets/pdf/modern_cpp_guide_Adam_Abed_Abud.pdf">Download the PDF here</a></p> <p>In the following section I will dive a bit deeper on the cool features of C++20.</p> <h3 id="concepts-a-giant-leap-for-template-clarity">Concepts: A Giant Leap for Template Clarity</h3> <p>Take <strong>Concepts</strong>, for example. No more cryptic template instantiation errors when you accidentally pass the wrong type. Now, you can explicitly constrain templates like this:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span> <span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">integral</span> <span class="n">T</span><span class="p">&gt;</span>
<span class="n">T</span> <span class="nf">add</span><span class="p">(</span><span class="n">T</span> <span class="n">a</span><span class="p">,</span> <span class="n">T</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This not only improves readability but makes your code self-documenting. The compiler’s error messages also become vastly more understandable. Finally some useful changes for template programming!</p> <h3 id="ranges-functional-elegance-meets-performance">Ranges: Functional Elegance Meets Performance</h3> <p>Another feature I found compelling was the Ranges library. With range-based views and composable pipelines, you can write expressive and clean code like:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">auto</span> <span class="n">evenSquares</span> <span class="o">=</span> <span class="n">numbers</span> 
    <span class="o">|</span> <span class="n">std</span><span class="o">::</span><span class="n">views</span><span class="o">::</span><span class="n">filter</span><span class="p">([](</span><span class="kt">int</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">x</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="p">;</span> <span class="p">})</span>
    <span class="o">|</span> <span class="n">std</span><span class="o">::</span><span class="n">views</span><span class="o">::</span><span class="n">transform</span><span class="p">([](</span><span class="kt">int</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">x</span> <span class="o">*</span> <span class="n">x</span><span class="p">;</span> <span class="p">});</span>
</code></pre></div></div> <p>It’s lazy, elegant, and highly performant—bringing C++ closer to the clarity you often see in functional languages, but without sacrificing control. This is a nice feature that I am looking forward to start experimenting with.</p> <h3 id="coroutines-asynchronous-without-the-headache">Coroutines: Asynchronous Without the Headache</h3> <p>And then there are Coroutines. They open the door to writing non-blocking, asynchronous code in a synchronous style. Imagine building a generator that produces an infinite arithmetic sequence—without threads, without state machines—just with co_yield. I still do not fully understand how to use them.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Generator</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">countBy</span><span class="p">(</span><span class="kt">int</span> <span class="n">start</span><span class="p">,</span> <span class="kt">int</span> <span class="n">step</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">value</span> <span class="o">=</span> <span class="n">start</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">co_yield</span> <span class="n">value</span><span class="p">;</span>
        <span class="n">value</span> <span class="o">+=</span> <span class="n">step</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>This is all powered by sophisticated compiler magic under the hood.</p> <h2 id="conclusion">Conclusion</h2> <p>I found writing this guidebook very interesting and useful for myself to review some features of C++ that I tend to not use. I feel it’s important to refresh such concepts from time to time. I hope the guide will also be useful. I used AI to compile some examples and I have to admit that I am quite satisfied on how it turned out. It was also a while I didn’t write such a technical material.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[from C++11 to C++20]]></summary></entry><entry><title type="html">Monte Carlo techniques for stock price estimation</title><link href="https://adam-abed-abud.github.io/blog/2025/code/" rel="alternate" type="text/html" title="Monte Carlo techniques for stock price estimation"/><published>2025-03-30T15:09:00+00:00</published><updated>2025-03-30T15:09:00+00:00</updated><id>https://adam-abed-abud.github.io/blog/2025/code</id><content type="html" xml:base="https://adam-abed-abud.github.io/blog/2025/code/"><![CDATA[<p>This project implements a Monte Carlo Simulation for Stock Price Prediction. This post is (heavily?) inspired from <a href="https://github.com/dataman-git/codes_for_articles/blob/master/time_series_08_monte_carlo_simulation.ipynb?utm_source=ayratmurtazin.beehiiv.com&amp;utm_medium=newsletter&amp;utm_campaign=monte-carlo-simulation-for-time-series-probabilistic-forecasting&amp;_bhlid=41146da02aef82ddd8485d69776814d56e3ab6fe">GuruFinance Insighs</a> and by an old piece of code that I used for estimateing the benefits of some life insurance offers.</p> <h2 id="introduction">Introduction</h2> <p>Monte Carlo simulation is a powerful technique used in finance to model the probability of different outcomes in processes that involve random variables. In this post, I’ll walk through how to implement Monte Carlo simulations for stock price prediction using Python, using both normal distribution and Student’s t-distribution approaches.</p> <p>A personal note: I have always been fascinated by the use of Monte Carlo methods, especially in Finance. I was also always surprized by the fact that not a lot of people around me know about them, yet they are so powerful and widly used. In my university years I used to experiment a lot with such techniques. Here is a presentation I did more than 7 years ago in Italy:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mcmc1-480.webp 480w,/assets/img/projects/monte_carlo/mcmc1-800.webp 800w,/assets/img/projects/monte_carlo/mcmc1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mcmc1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mcmc2-480.webp 480w,/assets/img/projects/monte_carlo/mcmc2-800.webp 800w,/assets/img/projects/monte_carlo/mcmc2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mcmc2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mcmc3-480.webp 480w,/assets/img/projects/monte_carlo/mcmc3-800.webp 800w,/assets/img/projects/monte_carlo/mcmc3-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mcmc3.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>I like Monte Carlo methods because it allow us to simulate thousands of possible future price paths for a stock, helping us understand the range of potential outcomes and their probabilities. This is particularly valuable for risk management, option pricing, and portfolio optimization. I don’t have many details about such field but I believe they must apply something similar.</p> <h2 id="implementation">Implementation</h2> <p>The implementation uses several Python libraries including pandas for data manipulation, yfinance for stock data retrieval, and matplotlib for visualization. Here’s how it works:</p> <ol> <li>Download the historical gold data of 1 oz</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Download data
</span><span class="n">stock_gold</span> <span class="o">=</span> <span class="sh">"</span><span class="s">GC=F</span><span class="sh">"</span>

<span class="n">stock</span> <span class="o">=</span> <span class="n">stock_gold</span>
<span class="n">data</span> <span class="o">=</span> <span class="n">yf</span><span class="p">.</span><span class="nf">download</span><span class="p">(</span><span class="n">stock</span><span class="p">,</span> <span class="n">start</span><span class="o">=</span><span class="sh">"</span><span class="s">2018-01-01</span><span class="sh">"</span><span class="p">,</span> <span class="n">end</span><span class="o">=</span><span class="sh">"</span><span class="s">2025-03-28</span><span class="sh">"</span><span class="p">)</span>

<span class="n">adj_close</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="sh">'</span><span class="s">Close</span><span class="sh">'</span><span class="p">]</span>
<span class="n">returns</span> <span class="o">=</span> <span class="n">adj_close</span><span class="p">.</span><span class="nf">pct_change</span><span class="p">()</span>

</code></pre></div></div> <p>This will yield the following:</p> <div class="row justify-content-sm-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/price_gold-480.webp 480w,/assets/img/projects/monte_carlo/price_gold-800.webp 800w,/assets/img/projects/monte_carlo/price_gold-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/price_gold.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/percentage_change-480.webp 480w,/assets/img/projects/monte_carlo/percentage_change-800.webp 800w,/assets/img/projects/monte_carlo/percentage_change-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/percentage_change.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Price of 1oz of gold in the last 5 years. </div> <p>The last value as of March 28th is <code class="language-plaintext highlighter-rouge">3060</code> USD.</p> <h3 id="monte-carlo-mc-simulation">Monte Carlo (MC) simulation</h3> <p>Here comes the fun part. The MC simulation consists in simulating different outcomes for the price variation. We do 1000 simulations. For that we take the standard deviation of the historical data for the last 5 years (maybe not the best estimator?) and then we use it to get an estimation of the forecasted percentage change of price by taking a random number (normal distribution with the same std of the real std of the historical data).This is based on the assumption that stock returns follow a random walk.</p> <p>We therefore can estimate the simulated price for the next year.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_simulations</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">num_days</span> <span class="o">=</span> <span class="mi">365</span>
<span class="n">daily_volatility</span> <span class="o">=</span> <span class="n">returns</span><span class="p">.</span><span class="nf">std</span><span class="p">()</span>

<span class="n">simulation_df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">()</span>
<span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_simulations</span><span class="p">):</span>
    <span class="n">count</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">price_series</span> <span class="o">=</span> <span class="p">[]</span> 
    <span class="n">rtn</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">daily_volatility</span><span class="p">)</span>
    <span class="n">price</span> <span class="o">=</span> <span class="n">last_price</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span><span class="o">+</span><span class="n">rtn</span><span class="p">)</span>
    <span class="n">price_series</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">price</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">days</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_days</span><span class="p">):</span>
        <span class="n">rtn</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">daily_volatility</span><span class="p">)</span>
        <span class="n">price</span> <span class="o">=</span> <span class="n">price_series</span><span class="p">[</span><span class="n">days</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span><span class="o">+</span><span class="n">rtn</span><span class="p">)</span>
        <span class="n">price_series</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">price</span><span class="p">)</span>

    <span class="n">simulation_df</span><span class="p">[</span><span class="n">x</span><span class="p">]</span> <span class="o">=</span> <span class="n">price_series</span>
</code></pre></div></div> <h3 id="results-of-the-simulation">Results of the simulation</h3> <p>Here are the results of the simulation</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mc_simulation-480.webp 480w,/assets/img/projects/monte_carlo/mc_simulation-800.webp 800w,/assets/img/projects/monte_carlo/mc_simulation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mc_simulation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MC simulation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Results of the MC simulation with 1000 trials </div> <p>It is alsom more interesting to see them as a function of the top and bottom quantiles, in other words what is the best case scenario and the worst case scenario.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mc_simulation_quantiles-480.webp 480w,/assets/img/projects/monte_carlo/mc_simulation_quantiles-800.webp 800w,/assets/img/projects/monte_carlo/mc_simulation_quantiles-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mc_simulation_quantiles.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MC simulation quantiles" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h3 id="monte-carlo-simulation-with-students-t-distribution">Monte Carlo Simulation with Student’s t-Distribution</h3> <p>While the normal distribution is commonly used, it often underestimates the probability of extreme events (fat tails) in financial markets. The Student’s t-distribution can better capture these tail risks. First, let’s fit a t-distribution to our historical returns:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Remove NaN values from returns
</span><span class="n">returns</span> <span class="o">=</span> <span class="n">returns</span><span class="p">[</span><span class="mi">1</span><span class="p">::]</span>

<span class="c1"># Fit t-distribution to returns
</span><span class="n">params</span> <span class="o">=</span> <span class="n">t</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">returns</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">degree of freedom: </span><span class="sh">'</span><span class="p">,</span> <span class="n">params</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">mean: </span><span class="sh">'</span><span class="p">,</span> <span class="n">params</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">standard deviation: </span><span class="sh">'</span><span class="p">,</span> <span class="n">params</span><span class="p">[</span><span class="mi">2</span><span class="p">])</span>
</code></pre></div></div> <p>I used the t-student distribution from <code class="language-plaintext highlighter-rouge">scipy.stats</code></p> <p>The following plot shows the distribution of the percentage change in price for gold (1 oz). Here is a plot that shows the actual distribution vs the t-student distribution:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/t_student-480.webp 480w,/assets/img/projects/monte_carlo/t_student-800.webp 800w,/assets/img/projects/monte_carlo/t_student-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/t_student.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MC simulation quantiles" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The following step is to do the MC simulation but with parameters extracted form the t-student distribtuion:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_simulations</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">num_days</span> <span class="o">=</span> <span class="mi">365</span>

<span class="n">simulation_student_t</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">()</span>

<span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_simulations</span><span class="p">):</span>
    <span class="n">count</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">price_series</span> <span class="o">=</span> <span class="p">[]</span> 
    <span class="n">rtn</span> <span class="o">=</span> <span class="n">t</span><span class="p">.</span><span class="nf">rvs</span><span class="p">(</span><span class="n">df</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">loc</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">scale</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">2</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> 
    <span class="n">price</span> <span class="o">=</span> <span class="n">last_price</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span><span class="o">+</span><span class="n">rtn</span><span class="p">)</span>
    <span class="n">price_series</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">price</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">days</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_days</span><span class="p">):</span>
        <span class="n">rtn</span> <span class="o">=</span> <span class="n">t</span><span class="p">.</span><span class="nf">rvs</span><span class="p">(</span><span class="n">df</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">loc</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">scale</span><span class="o">=</span><span class="n">params</span><span class="p">[</span><span class="mi">2</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> 
        <span class="n">price</span> <span class="o">=</span> <span class="n">price_series</span><span class="p">[</span><span class="n">days</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span><span class="o">+</span><span class="n">rtn</span><span class="p">)</span>
        <span class="n">price_series</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">price</span><span class="p">)</span>
    <span class="n">simulation_student_t</span><span class="p">[</span><span class="n">x</span><span class="p">]</span> <span class="o">=</span> <span class="n">price_series</span>
</code></pre></div></div> <p>And here are the results:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mc_simulation_tsudent-480.webp 480w,/assets/img/projects/monte_carlo/mc_simulation_tsudent-800.webp 800w,/assets/img/projects/monte_carlo/mc_simulation_tsudent-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mc_simulation_tsudent.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MC simulation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Results of the MC simulation with 1000 trials using the t-student distribution </div> <p>And here are the top and bottom quantiles:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/projects/monte_carlo/mc_simulation_tsudent_quantiles-480.webp 480w,/assets/img/projects/monte_carlo/mc_simulation_tsudent_quantiles-800.webp 800w,/assets/img/projects/monte_carlo/mc_simulation_tsudent_quantiles-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/projects/monte_carlo/mc_simulation_tsudent_quantiles.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MC simulation quantiles" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The key difference between these two approaches is how they handle extreme events:</p> <ul> <li> <p>Normal distribution: Assumes returns are symmetrically distributed around the mean with thin tails.</p> </li> <li> <p>Student’s t-distribution: Can better model the fat tails observed in real financial returns, capturing the higher probability of extreme events.</p> </li> </ul> <p>In practice, you’ll often notice that the t-distribution simulation shows a wider range of potential outcomes, especially in the extreme cases, which could be more realistic for risk management purposes.</p> <h2 id="conclusion">Conclusion</h2> <p>Monte Carlo simulation is a versatile technique for modeling uncertainty in financial markets. By generating thousands of possible future price paths, we can better understand the range of potential outcomes and make more informed investment decisions. I personally use it frequently (maybe too often!).</p> <p>The choice between normal and t-distribution depends on your specific needs:</p> <ul> <li>Normal distribution is simpler and works well for many applications</li> <li>T-distribution better captures extreme events and is often more appropriate for risk management</li> </ul> <p>This approach can be extended to multiple assets, different time horizons, and more complex financial instruments like options and structured products.</p> <p>The full code for this example can be found in this github repository with the python notebook (<a href="assets/img/projects/monte_carlo/monte_carlo.ipynb">here</a>).</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Analyzing stock values using MC techniques]]></summary></entry></feed>